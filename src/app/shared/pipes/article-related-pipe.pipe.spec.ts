import { ArticleRelatedPipePipe } from './article-related-pipe.pipe';

describe('ArticleRelatedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleRelatedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
