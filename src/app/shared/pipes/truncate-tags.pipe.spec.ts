import { TruncateTagsPipe } from './truncate-tags.pipe';

describe('TruncateTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
